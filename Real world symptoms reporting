#User's input raw sentence
import nltk
nltk.download('punkt_tab')
from nltk.tokenize import word_tokenize



user_input = str(input("Your symptoms please 😊: "))
user_input = user_input.lower()
tokenized_input = word_tokenize(user_input)
print(tokenized_input)

no_punctuation = [ ]
import string
punctuation_marks = string.punctuation 
from nltk import PorterStemmer
stem_instance = PorterStemmer()
for word in tokenized_input:
  if(word not in punctuation_marks):
    no_punctuation.append(word)
normalized_sentence = [ ]
for w in no_punctuation:
  stemmed = stem_instance.stem(w)
  normalized_sentence.append(stemmed)
print(normalized_sentence)


itum_columns = indra_chowk.columns
whatever = [ ]
for w in itum_columns:
  whatever.append(w)
whatever.remove('Disease')
whatever.remove('Group ID')
print(whatever)

itum_bahal = pd.DataFrame(data=0,index=[0],columns=whatever)
display(itum_bahal)

